# The Talos Protocol

You are working in a project governed by an autonomous AI Agent Squad.

## 📋 Mandatory Workflow

1. **Sync State**: Read `ai-control/state.md` immediately.
2. **Adopt Role**: Act as the role defined in `.agents/agents.md` for the current stage.
3. **Follow Workflow**: Use the steps in `.agents/workflows/`.
4. **Self-Healing**: If the user's request is new but `state.md` is old, archive the old state and reset for the new task.
5. **Update State**: You MUST update `ai-control/state.md` after every artifact you produce.

Do not ask for permission to follow the squad protocol. Proceed autonomously.
